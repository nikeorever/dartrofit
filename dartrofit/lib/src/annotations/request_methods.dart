/// make a GET request
class GET {
  final String relativeUrl;

  const GET([this.relativeUrl]);
}

/// make a POST request
class POST {
  final String relativeUrl;

  const POST([this.relativeUrl]);
}

/// make a PUT request
class PUT {
  final String relativeUrl;

  const PUT([this.relativeUrl]);
}

/// make a PATCH request
class PATCH {
  final String relativeUrl;

  const PATCH([this.relativeUrl]);
}

/// make a DELETE request
class DELETE {
  final String relativeUrl;

  const DELETE([this.relativeUrl]);
}

/// make a HEAD request
class HEAD {
  final String relativeUrl;

  const HEAD([this.relativeUrl]);
}

/// make a OPTIONS request
class OPTIONS {
  final String relativeUrl;

  const OPTIONS([this.relativeUrl]);
}
